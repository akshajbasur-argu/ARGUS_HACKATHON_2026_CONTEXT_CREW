from fastapi import APIRouter

router = APIRouter()

# TODO: GET    /          — list grant programmes (paginated)
# TODO: POST   /          — create a new programme
# TODO: GET    /{id}      — get programme detail
# TODO: PUT    /{id}      — update programme
# TODO: DELETE /{id}      — archive programme
# TODO: POST   /{id}/open — open programme for applications
# TODO: POST   /{id}/close — close programme
