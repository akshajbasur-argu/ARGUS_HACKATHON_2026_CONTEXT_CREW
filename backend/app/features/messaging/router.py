from fastapi import APIRouter

router = APIRouter()

# TODO: GET  /threads                — list message threads for current user
# TODO: POST /threads                — start a new thread (application-scoped or general)
# TODO: GET  /threads/{id}/messages  — get messages in a thread
# TODO: POST /threads/{id}/messages  — send a message
# TODO: POST /notifications          — create a system notification
# TODO: GET  /notifications          — list notifications for current user
# TODO: POST /notifications/{id}/read — mark notification as read
