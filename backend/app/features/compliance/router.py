from fastapi import APIRouter

router = APIRouter()

# TODO: GET  /requirements                   — list compliance requirements for a programme
# TODO: POST /requirements                   — create a compliance requirement
# TODO: GET  /submissions                    — list compliance submissions by awardee
# TODO: POST /submissions                    — submit a compliance report / document
# TODO: GET  /submissions/{id}              — get submission detail
# TODO: POST /submissions/{id}/approve      — mark submission as compliant
# TODO: POST /submissions/{id}/reject       — mark submission as non-compliant
