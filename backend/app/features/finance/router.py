from fastapi import APIRouter

router = APIRouter()

# TODO: GET  /disbursements          — list disbursement records
# TODO: POST /disbursements          — record a disbursement
# TODO: GET  /disbursements/{id}     — get disbursement detail
# TODO: POST /disbursements/{id}/confirm — confirm payment processed
# TODO: GET  /budgets                — budget utilisation summary per programme
# TODO: GET  /reports                — financial reports (CSV/JSON export)
