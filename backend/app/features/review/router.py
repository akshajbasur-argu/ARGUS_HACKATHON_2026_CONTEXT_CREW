from fastapi import APIRouter

router = APIRouter()

# TODO: GET  /panels                   — list review panels
# TODO: POST /panels                   — create a review panel
# TODO: POST /panels/{id}/assign       — assign reviewers to a panel
# TODO: GET  /assignments              — list reviewer's assignments
# TODO: POST /assignments/{id}/score   — submit a score + comments
# TODO: GET  /applications/{id}/scores — aggregate scores for an application
