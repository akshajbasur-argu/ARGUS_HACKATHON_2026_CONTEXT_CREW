from fastapi import APIRouter

router = APIRouter()

# TODO: GET  /                  — list awards
# TODO: POST /                  — create an award for an approved application
# TODO: GET  /{id}              — get award detail
# TODO: PUT  /{id}              — update award (amount, conditions)
# TODO: POST /{id}/accept       — applicant accepts award
# TODO: POST /{id}/decline      — applicant declines award
# TODO: GET  /{id}/tranches     — list payment tranches for an award
